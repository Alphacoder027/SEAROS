# Models Directory

This directory stores trained ML models.

## Files
- `yield_model.pkl` - Trained Random Forest model for yield prediction (generated by `train_model.py`)

## Training
Run `python train_model.py` from the project root to train and save the model.
